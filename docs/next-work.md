# LeanTTY 当前工作

> 状态：唯一有效的项目 TODO
>
> 更新日期：2026-07-28
>
> 上位规则：[`project-principles.md`](project-principles.md)

## 当前结论

LeanTTY `1.0.1` / `1000001` 是首个实际公开发布候选。被拒绝且从未发布的 1.0.0、
公开源码基线、全面更名、测试签名、Host/Key 恢复和旧身份清理均已完成；完成事实
记录在 `CHANGELOG.md`、Git 历史和仓库外私有发布证据中，不在本文件维护第二份
历史清单。

两份 1.1 技术草案尚未授权实现。只有明确决定进入当前版本后，相关工作才能写入
本文件并开始执行。

## P0：1.0.1 发布验收

- [ ] 在 ARM64 HarmonyOS PC 上完成最终人工验收：核对应用名称、青薄荷图标、
  `ltty>` 提示符、Bundle、Preferences、SSH 文件和窗口状态；使用物理键盘覆盖
  连接、认证、主机校验、断线、重连、Tab、双 Pane、复制粘贴、字号、主题、
  常见 TUI 和终端链接核心路径。自动安装启动或 `-SkipDevice` 不能替代该验收。
- [ ] 从干净 `main` 冻结同一份 `LeanTTY-1.0.1` 生产签名候选，记录 commit、tree、
  版本、APP/HAP/native/manifest 哈希和签名校验结果；使用这份不再变化的候选完成
  AppGallery 自测录屏、提交材料和审核提交。候选变化时必须重新编号、验证和录制。

## P1：工程验证工具可靠性

- [ ] 修复自动化 SSH smoke 的终端文本注入。当前工具曾把预期的 `echo` 注入为
  `eho`；同一版本使用物理键盘手动输入完整、远端输出正常，因此该现象归类为测试
  工具缺陷，不是 LeanTTY 键盘输入缺陷。修复前，自动注入产生的命令文本和数据面
  断言不得作为验收证据，相关场景继续以真机物理键盘验证为准。

## 文档规则

- 本文件是唯一活动任务清单，只保留未完成工作。
- `project-principles.md` 是产品和技术决策的最高规则。
- 已完成工作进入 `CHANGELOG.md` 和 Git 历史，不在此保留大段完成记录。
- 历史方案、归档材料和 1.1 草案不自动授权实现。
